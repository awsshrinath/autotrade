apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.optionsTrader.name }}
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.optionsTrader.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.optionsTrader.name }}
    spec:
      serviceAccountName: {{ .Values.serviceAccountName }}
      containers:
        - name: {{ .Values.optionsTrader.name }}
          image: "{{ .Values.optionsTrader.image.repository }}:{{ .Values.optionsTrader.image.tag }}"
          command: ["/bin/bash"]
          args: ["/entrypoint.sh"]
          resources:
            {{- toYaml .Values.optionsTrader.resources | nindent 12 }}
          env:
            {{- range $key, $val := .Values.optionsTrader.env }}
            - name: {{ $key }}
              value: "{{ $val }}"
            {{- end }}
            - name: GCP_PROJECT_ID
              value: "{{ .Values.gcpProjectId }}"
            - name: PYTHONPATH
              value: "/app:/app/runner:/app/options_trading"
          volumeMounts:
            - name: entrypoint
              mountPath: /entrypoint.sh
              subPath: entrypoint.sh
      volumes:
        - name: entrypoint
          configMap:
            name: entrypoint-config
            defaultMode: 0755 