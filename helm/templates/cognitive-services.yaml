# TRON Trading System - Cognitive Services
# Cognitive Engine, Thought Journal, and Memory Manager deployments

{{- if .Values.cognitiveEngine.enabled }}
---
# Cognitive Engine Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.cognitiveEngine.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.cognitiveEngine.name }}
    app.kubernetes.io/component: cognitive-system
    app.kubernetes.io/part-of: tron-trading-system
spec:
  replicas: {{ .Values.cognitiveEngine.replicaCount | default .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.cognitiveEngine.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.cognitiveEngine.name }}
    spec:
      serviceAccountName: {{ .Values.serviceAccountName }}
      containers:
        - name: {{ .Values.cognitiveEngine.name }}
          image: "{{ .Values.cognitiveEngine.image.repository }}:{{ .Values.global.imageTag | default .Values.cognitiveEngine.image.tag }}"
          imagePullPolicy: {{ .Values.cognitiveEngine.image.pullPolicy }}
          command: ["/bin/bash", "/app/entrypoint.sh"]
          args: ["runner/cognitive_system.py"]
          ports:
            - containerPort: {{ .Values.cognitiveEngine.service.targetPort }}
              name: http
          env:
            - name: GCP_PROJECT_ID
              valueFrom:
                configMapKeyRef:
                  name: tron-system-config
                  key: GCP_PROJECT_ID
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: tron-system-config
                  key: ENVIRONMENT
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: tron-system-config
                  key: LOG_LEVEL
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: api-secrets
                  key: openai-api-key
                  optional: true
            {{- range $key, $val := .Values.cognitiveEngine.env }}
            - name: {{ $key }}
              value: "{{ $val }}"
            {{- end }}
            - name: PYTHONPATH
              value: "/app:/app/runner"
            - name: HEALTH_CHECK_ENABLED
              value: "{{ .Values.cognitiveEngine.healthCheck.enabled }}"
            - name: SERVICE_PORT
              value: "{{ .Values.cognitiveEngine.service.targetPort }}"
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: "/etc/gcp/key.json"
            - name: FIRESTORE_PROJECT_ID
              valueFrom:
                configMapKeyRef:
                  name: tron-system-config
                  key: GCP_PROJECT_ID
            - name: GOOGLE_CLOUD_PROJECT
              valueFrom:
                configMapKeyRef:
                  name: tron-system-config
                  key: GCP_PROJECT_ID
          resources:
            {{- toYaml .Values.cognitiveEngine.resources | nindent 12 }}
          {{- if .Values.cognitiveEngine.healthCheck.enabled }}
          readinessProbe:
            httpGet:
              path: {{ .Values.cognitiveEngine.healthCheck.path }}
              port: {{ .Values.cognitiveEngine.healthCheck.port }}
            initialDelaySeconds: 120
            periodSeconds: 60
            timeoutSeconds: 15
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: {{ .Values.cognitiveEngine.healthCheck.path }}
              port: {{ .Values.cognitiveEngine.healthCheck.port }}
            initialDelaySeconds: 180
            periodSeconds: 120
            timeoutSeconds: 20
            failureThreshold: 5
          {{- end }}
          volumeMounts:
            - name: entrypoint
              mountPath: /app/entrypoint.sh
              subPath: entrypoint.sh
            - name: gcp-service-account-key
              mountPath: /etc/gcp
              readOnly: true
      volumes:
        - name: entrypoint
          configMap:
            name: entrypoint-config
            defaultMode: 0755
        - name: gcp-service-account-key
          secret:
            secretName: gcp-service-account-key
            optional: true
      restartPolicy: Always

---
# Cognitive Engine Service
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.cognitiveEngine.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.cognitiveEngine.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.cognitiveEngine.service.port }}
      targetPort: {{ .Values.cognitiveEngine.service.targetPort }}
  type: {{ .Values.cognitiveEngine.service.type | default "ClusterIP" }}
{{- end }}

{{- if .Values.thoughtJournal.enabled }}
---
# Thought Journal Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.thoughtJournal.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.thoughtJournal.name }}
    app.kubernetes.io/component: cognitive-system
    app.kubernetes.io/part-of: tron-trading-system
spec:
  replicas: {{ .Values.thoughtJournal.replicaCount | default .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.thoughtJournal.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.thoughtJournal.name }}
    spec:
      serviceAccountName: {{ .Values.serviceAccountName }}
      containers:
        - name: {{ .Values.thoughtJournal.name }}
          image: "{{ .Values.thoughtJournal.image.repository }}:{{ .Values.global.imageTag | default .Values.thoughtJournal.image.tag }}"
          imagePullPolicy: {{ .Values.thoughtJournal.image.pullPolicy }}
          command: ["/bin/bash", "/app/entrypoint.sh"]
          args: ["runner/thought_journal.py"]
          ports:
            - containerPort: {{ .Values.thoughtJournal.service.targetPort }}
              name: http
          env:
            - name: GCP_PROJECT_ID
              valueFrom:
                configMapKeyRef:
                  name: tron-system-config
                  key: GCP_PROJECT_ID
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: tron-system-config
                  key: ENVIRONMENT
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: tron-system-config
                  key: LOG_LEVEL
            {{- range $key, $val := .Values.thoughtJournal.env }}
            - name: {{ $key }}
              value: "{{ $val }}"
            {{- end }}
            - name: PYTHONPATH
              value: "/app:/app/runner"
            - name: HEALTH_CHECK_ENABLED
              value: "{{ .Values.thoughtJournal.healthCheck.enabled }}"
            - name: SERVICE_PORT
              value: "{{ .Values.thoughtJournal.service.targetPort }}"
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: "/etc/gcp/key.json"
            - name: FIRESTORE_PROJECT_ID
              valueFrom:
                configMapKeyRef:
                  name: tron-system-config
                  key: GCP_PROJECT_ID
            - name: GOOGLE_CLOUD_PROJECT
              valueFrom:
                configMapKeyRef:
                  name: tron-system-config
                  key: GCP_PROJECT_ID
          resources:
            {{- toYaml .Values.thoughtJournal.resources | nindent 12 }}
          {{- if .Values.thoughtJournal.healthCheck.enabled }}
          readinessProbe:
            httpGet:
              path: {{ .Values.thoughtJournal.healthCheck.path }}
              port: {{ .Values.thoughtJournal.healthCheck.port }}
            initialDelaySeconds: 120
            periodSeconds: 60
            timeoutSeconds: 15
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: {{ .Values.thoughtJournal.healthCheck.path }}
              port: {{ .Values.thoughtJournal.healthCheck.port }}
            initialDelaySeconds: 180
            periodSeconds: 120
            timeoutSeconds: 20
            failureThreshold: 5
          {{- end }}
          volumeMounts:
            - name: entrypoint
              mountPath: /app/entrypoint.sh
              subPath: entrypoint.sh
            - name: gcp-service-account-key
              mountPath: /etc/gcp
              readOnly: true
      volumes:
        - name: entrypoint
          configMap:
            name: entrypoint-config
            defaultMode: 0755
        - name: gcp-service-account-key
          secret:
            secretName: gcp-service-account-key
            optional: true
      restartPolicy: Always

---
# Thought Journal Service
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.thoughtJournal.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.thoughtJournal.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.thoughtJournal.service.port }}
      targetPort: {{ .Values.thoughtJournal.service.targetPort }}
  type: {{ .Values.thoughtJournal.service.type | default "ClusterIP" }}
{{- end }}

{{- if .Values.memoryManager.enabled }}
---
# Memory Manager Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.memoryManager.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.memoryManager.name }}
    app.kubernetes.io/component: cognitive-system
    app.kubernetes.io/part-of: tron-trading-system
spec:
  replicas: {{ .Values.memoryManager.replicaCount | default .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.memoryManager.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.memoryManager.name }}
    spec:
      serviceAccountName: {{ .Values.serviceAccountName }}
      containers:
        - name: {{ .Values.memoryManager.name }}
          image: "{{ .Values.memoryManager.image.repository }}:{{ .Values.global.imageTag | default .Values.memoryManager.image.tag }}"
          imagePullPolicy: {{ .Values.memoryManager.image.pullPolicy }}
          command: ["/bin/bash", "/app/entrypoint.sh"]
          args: ["runner/cognitive_memory.py"]
          ports:
            - containerPort: {{ .Values.memoryManager.service.targetPort }}
              name: http
          env:
            - name: GCP_PROJECT_ID
              valueFrom:
                configMapKeyRef:
                  name: tron-system-config
                  key: GCP_PROJECT_ID
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: tron-system-config
                  key: ENVIRONMENT
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: tron-system-config
                  key: LOG_LEVEL
            {{- range $key, $val := .Values.memoryManager.env }}
            - name: {{ $key }}
              value: "{{ $val }}"
            {{- end }}
            - name: PYTHONPATH
              value: "/app:/app/runner"
            - name: HEALTH_CHECK_ENABLED
              value: "{{ .Values.memoryManager.healthCheck.enabled }}"
            - name: SERVICE_PORT
              value: "{{ .Values.memoryManager.service.targetPort }}"
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: "/etc/gcp/key.json"
            - name: FIRESTORE_PROJECT_ID
              valueFrom:
                configMapKeyRef:
                  name: tron-system-config
                  key: GCP_PROJECT_ID
            - name: GOOGLE_CLOUD_PROJECT
              valueFrom:
                configMapKeyRef:
                  name: tron-system-config
                  key: GCP_PROJECT_ID
          resources:
            {{- toYaml .Values.memoryManager.resources | nindent 12 }}
          {{- if .Values.memoryManager.healthCheck.enabled }}
          readinessProbe:
            httpGet:
              path: {{ .Values.memoryManager.healthCheck.path }}
              port: {{ .Values.memoryManager.healthCheck.port }}
            initialDelaySeconds: 120
            periodSeconds: 60
            timeoutSeconds: 15
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: {{ .Values.memoryManager.healthCheck.path }}
              port: {{ .Values.memoryManager.healthCheck.port }}
            initialDelaySeconds: 180
            periodSeconds: 120
            timeoutSeconds: 20
            failureThreshold: 5
          {{- end }}
          volumeMounts:
            - name: entrypoint
              mountPath: /app/entrypoint.sh
              subPath: entrypoint.sh
            - name: gcp-service-account-key
              mountPath: /etc/gcp
              readOnly: true
      volumes:
        - name: entrypoint
          configMap:
            name: entrypoint-config
            defaultMode: 0755
        - name: gcp-service-account-key
          secret:
            secretName: gcp-service-account-key
            optional: true
      restartPolicy: Always

---
# Memory Manager Service
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.memoryManager.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.memoryManager.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.memoryManager.service.port }}
      targetPort: {{ .Values.memoryManager.service.targetPort }}
  type: {{ .Values.memoryManager.service.type | default "ClusterIP" }}
{{- end }}