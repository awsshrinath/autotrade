apiVersion: batch/v1
kind: Job
metadata:
  name: futures-trader
  namespace: gpt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: futures-trader
  template:
    metadata:
      labels:
        app: futures-trader
    spec:
      containers:
        - name: futures-trader
          image: asia-south1-docker.pkg.dev/autotrade-453303/gpt-repo/futures-trader:latest
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
